{
  "name": "npm-example",
  "version": "1.0.0",
  "description": "",
  "main": "test1.js",
  "scripts": {
    "prebuild": "echo \"Kicking off a Build\"",
    "build": "npm run test:spec && echo \"Run the build\"",
    "postbuild": "echo \"Done!\" && exit 0",
    "test": "mocha test/",
    "test:spec": "npm run test -- --reporter spec"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mocha": "^2.2.5"
  }
}
